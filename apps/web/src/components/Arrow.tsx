import { MoveRight } from 'lucide-react';

const Arrow = () => (
    <div className="surface onSurface rounded-lg border border-gray-600 h-12 w-12 flex items-center justify-center">
        <MoveRight />
    </div>
);

export default Arrow;
